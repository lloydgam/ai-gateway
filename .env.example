# Server
PORT=8000
NODE_ENV=development

# Provider
ANTHROPIC_API_KEY=replace_me

# Postgres
DATABASE_URL=postgresql://postgres:postgres@db:5432/aigateway?schema=public

# Key hashing (change in prod)
GATEWAY_KEY_SALT=change_me_to_a_long_random_string

# Model alias mapping
CLAUDE_FAST_MODEL=claude-3-5-haiku-latest
CLAUDE_QUALITY_MODEL=claude-3-5-sonnet-latest
CLAUDE_PREMIUM_MODEL=claude-3-opus-latest

# Budgets (USD) — enforced per API key per calendar month
DEFAULT_MONTHLY_LIMIT_USD=200.00
ENFORCE_BUDGETS=true
